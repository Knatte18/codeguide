# Project Instructions

## Git

This repo uses a single-branch workflow. Committing to main is allowed — when `/mill-commit` is invoked without `--onmain`, treat it as if `--onmain` was passed. Only push when explicitly asked to push.
